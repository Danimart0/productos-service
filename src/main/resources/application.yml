server:
  port: 8081
spring:
  application:
    name: productos-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/productos_db}
  kafka:
    bootstrap-servers: ${KAFKA_URI:localhost:9092}
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
management:
  endpoints:
    web:
      exposure:
        include: "*"
